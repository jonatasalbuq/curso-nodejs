<h1 class="h3">Lista de categorias:</h1>
{{#each categorias}}
    <div class="card mt-4">
        <div class="card-body">
            <a href="/categorias/{{{slug}}}"><h1 class="h4">{{nome}}</h1></a>
        </div>
    </div>
{{else}}
    <p>Não há categorias registradas no momento.</p>
{{/each}}